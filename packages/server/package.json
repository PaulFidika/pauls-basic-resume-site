{
  "name": "paul-resume-server",
  "version": "1.0.0",
  "dependencies": {
    "cross-env": "5.0.5",
    "express": "^4.17.3"
  },
  "main": "dist/main.js",
  "scripts": {
    "dev": "nodemon --watch 'src/**' --ext 'ts,json' --ignore 'src/**/*.spec.ts' --exec 'ts-node src/main.ts'",
    "build": "tsc",
    "start": "node ."
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.23",
    "nodemon": "^2.0.15",
    "typescript": "^4.6.3"
  }
}
